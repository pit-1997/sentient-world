export const constants = {
  /**
   * Интервал проверки смены даты
   *
   * Определяет как часто World проверяет переход через полночь
   * для увеличения счётчика дней
   *
   * @default 1000 (1 секунда реального времени)
   */
  DATE_TRACKING_INTERVAL: 1000,

  /**
   * Интервал обновления персонажей
   *
   * Определяет как часто вызывается tick() для каждого персонажа
   * для обновления их состояния и поведения
   *
   * @default 1000 (1 секунда реального времени)
   */
  CHARACTERS_TICK_INTERVAL: 1000,
};
